<div class="row">
  <div class="zoom col-md-4">
    <h1>New Appointment</h1>
    <%= render partial: 'form', locals: { appointment: @appointment, client: @client, template: @template } %>
  </div>
  <div id="fadeshow1" class="col-md-8">
    <%= add_weekly_calendar(@appointments) %>
  </div>
</div>
